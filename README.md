# MQTT Logger

usage:
```
1- Config the file src/main.py with your broker and topic.
2- Run the script, this one will create a file called 'data-YYYY-MM-DD.csv' where the messages will be stored.
3- After one day the script will detect the change and will create a new file.
```
